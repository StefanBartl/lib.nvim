# Tests

Headless spec suite for lib.nvim. Covers pure / buffer-level logic that is
testable without a UI.

## Run

From the repo root:

```sh
nvim --headless -u NONE -l TESTS/run.lua
```

or:

```sh
nvim --headless -u NONE -c "set rtp+=." -c "luafile TESTS/run.lua" -c "qa!"
```

The runner prints one line per spec and exits non-zero on the first failure
(`LIB_TESTS_OK` on success).

## Layout

The table below is in `run.lua`'s own load order. Entries carried over from
before this pass keep their original, more detailed wording; entries added or
reconciled in this pass are intentionally terser — a pointer to the module and
its major branches, not a full walkthrough. Several rows below existed in
`run.lua` long before they had a row here at all — this table had drifted
significantly out of sync with the actual spec list; that gap is closed now,
not carried forward.

| File              | Covers                                                          |
| ----------------- | --------------------------------------------------------------- |
| `harness.lua`     | Shared `eq`/`ok` assertions, `tmpfile()`, `read_lines()`.       |
| `polymorphic_rootresolver_spec.lua` | `lib.nvim.fs.polymorphic_rootresolver`: buffer-number/filename/nil argument normalization and the optional-callback contract an LSP `root_dir` resolver needs. |
| `logger_spec.lua` | `lib.nvim.logger`: records, level/tag/master switches, ring bound, redaction, guard/wrap, file sink (JSONL), flush/clear. |
| `autocmd_spec.lua` | `lib.nvim.bindings.autocmd`: `create` (buffer-vs-pattern exclusivity, defensive `pcall` wrapping, `raw` bypass), `group`/`get_augroup` caching including the re-clear-on-a-cached-name regression (a second `clear = true` call must still empty the group, not just the first), dead-group cache invalidation, `delete`/`registered` record-keeping, and `augroup.create.clear`. |
| `keymap_registry_spec.lua` | `lib.nvim.bindings.keymap`: action registration/lookup, buffer-local vs. global scoping, and the warn-once-per-typo'd-action path. |
| `count_spec.lua`  | `lib.nvim.count`: `get`/`raw`/`given`/`clamp` read through a real count-prefixed keypress (`nvim_feedkeys`, since `vim.v.count` is only meaningful inside a mapping), including an over-range count hitting the clamp. `times`: one run per count, the 1-based index, an early `false` stopping the loop and being reported honestly, explicit `max` and the `DEFAULT_MAX` fat-finger cap. `chain`: exactly one call fires up front and each further one only on `advance`, no extra call once the count is satisfied, unsubscribe after the last signal, no listener at all when count <= 1, `abort` dropping the chain and a stale `advance` afterwards not resurrecting it, a raising `subscribe` still running the action exactly once (never the unpaced loop), and a `subscribe` returning no unsubscribe. |
| `lastcmd_spec.lua` | `lib.nvim.lastcmd`, driven through real keypresses (`nvim_feedkeys` with `"mtx"`, since the tracker reads `vim.on_key`): a Lua-callback mapping is recorded with its typed count prefix and replayed at the new cursor; pure motions (`jj`, `5j`) neither run nor overwrite it; the most recently run mapping wins over an earlier one; a native change after a mapping wins over it and is repeated via `normal! .`, and a mapping run afterwards takes the lead back; the repeat key never records itself (callback-identity guard); `opts.ignore` suppresses a mapped motion; a multi-key mapping resolves through its prefix; a Visual mapping records its selection *shape* and replays against an equally sized selection at the cursor without overshooting. Opt-in behaviour: `setup()` installs nothing without `experimental`, binds the requested lhs with it, moves the binding when the lhs changes, and `experimental = false` tears both down. Two regressions are pinned: a trigger that wraps `repeat_last` in a closure (every documented binding does) must not record and then replay *itself* into a hang, and pressing the trigger repeatedly must keep replaying rather than silently degrading to `normal! .` from the second press on. Plus `setup` idempotence, `peek`/`clear`/`trigger_key`/`teardown`. |
| `keymap_modifier_spec.lua` | `lib.nvim.bindings.keymap.modifier`, driven through real keypresses so the `getcharstr()` read of the following keys is exercised: all four capture tiers against the mapping shapes they have to survive — a callback that returns its result, a buffer-local one that only moves a register, a *string* rhs with no callback to call at all, an opaque one that must still run while honestly reporting nothing to capture, and an `expr` mapping whose return value is keys and must never be mistaken for a result. Also `declare`/`undeclare` outranking and then falling back to the observed register, `\` and `\\` coexisting as a prefix pair (the short one still resolves when the next key is not another `\`), an unmapped target copying nothing, and the opt-in/teardown lifecycle. The tier-3 targets deliberately move the *unnamed* register while the assertions read `+`, so a passing test proves the modifier put it there — asserting `+` against a target that writes `+` passes with the capture logic removed entirely. |
| `autocmd_dispatcher_spec.lua` | `lib.nvim.bindings.autocmd.dispatcher`: register/attach/detach/stats, sort-at-registration (priority + stable registration-order tiebreak), glob key matching, per-buffer `once` keyed independently per registration (regression: two registrations sharing one loader don't satisfy each other's `once`), key-list registrations, `opts.context` threading, and `dispatcher.filetype`'s real `FileType` dispatch + default buffer-context. |
| `autocmd_docs_spec.lua` | `lib.nvim.bindings.autocmd.docs`: the generated-page writer refuses (rather than silently omitting) when a registration is unattributable, so the page never claims to be the whole list while quietly being short. |
| `usercmd_registry_spec.lua` | `lib.nvim.bindings.usercmd`'s registry and generated-docs writer: registration bookkeeping and the same "don't silently omit" contract as `autocmd_docs_spec.lua`, for user commands instead of autocmds. |
| `bindings_audit_spec.lua` | `lib.nvim.bindings.audit`: the repo-wide lint that flags a raw `nvim_create_autocmd`/`nvim_create_user_command`/keymap call site bypassing this module's own wrappers, including the `lib-docs: fallback` opt-out marker and string-literal/comment false-positive avoidance. |
| `keymap_portability_spec.lua` | `lib.nvim.bindings.keymap.portability` and the `bindings.audit` lint built on it: flags a keymap `rhs` that is not portable across the keyboard layouts/terminals this fleet targets. |
| `dev_duplicates_spec.lua` | `lib.nvim.dev.duplicates`: detecting two `require()`d copies of what should be one module (the failure mode `lib.nvim.dev.reload` exists to prevent from lingering after a hot-reload). |
| `dev_reload_spec.lua` | `lib.nvim.dev.reload`: clearing `package.loaded` for a module tree and re-requiring it, without leaving the stale copy `dev_duplicates_spec.lua` would flag. |
| `markdown_table_spec.lua` | `lib.nvim.markdown.table`: column-width computation and alignment-marker rendering, extracted from two independently-written copies that had already drifted apart — the assertions pin the union of both copies' correct behavior. |
| `run_argv_spec.lua` | `lib.nvim.cross.run_argv`: `run_blocking`/`run_blocking_captured` against real commands (success, non-zero exit, the ENOENT regression that used to raise synchronously instead of returning `(false, err)`). |
| `run_spec.lua`    | `lib.nvim.cross.run`: `shell()` (native-Windows-vs-WSL `is_powershell`), `run`/`run_blocking` end to end, and the `cross.run.env` enrichment wired in by default — the completed `PATH` and an `opts.env` var actually reaching a real spawned subprocess (async and blocking), and `opts.env = false` genuinely skipping it rather than just skipping the override. |
| `spawn_env_spec.lua` | `lib.nvim.cross.run.env`: aggregator wiring (`cross.run.env`, `lib.spawn_env`), `SESSION_VARS` (wsl aliasing linux, common-first ordering, fresh list per call), `missing()` against `vim.env`, `candidate_dirs()` (only existing dirs, cached), `path()` (nothing inherited dropped, every candidate present, no duplicates under the Windows-insensitive rule, `extra_paths` order, `opts.base`), `build()` (exactly one PATH key whatever its casing, `vars` precedence, `passthrough`), `apply()` (unrelated options and a caller `env` survive, input not mutated), `login_shell_env()` (nil on native Windows), `clear()`. |
| `async_spec.lua`  | `lib.nvim.async`: `await`/`run` against real `uv` timers (so a pass proves control really left and came back), multi-return with embedded `nil`, a body that never awaits, optional `on_done`; the error path (raised before *and* after the first await, `on_done` skipped); `wrap` against real `uv.fs_stat` plus a function with omitted optional args; `Semaphore` (critical sections provably never overlap, uncontended acquires don't deadlock, an uncontended release recycles the permit); `Condvar` (`notify_one` wakes exactly one, `notify_all` the rest, notifying an empty list is a no-op). |
| `async_walk_spec.lua` | The coroutine-driven `*_async` counterparts: `collect_recursive.collect_async`/`files_async`/`dirs_async` produce the exact same result set as their synchronous counterparts on a real fixture tree (including `ignore`-predicate pruning and a nonexistent-root settling to an empty list), `cancel()` genuinely suppresses `on_done`, and `scan_cached.scan_async`/`scan_roots.scan_async` carry the same TTL/cache-hit/refresh/multi-root semantics as their synchronous originals. |
| `wslpath_spec.lua` | `lib.nvim.cross.fs.wslpath`: the real WSL path conversion can only be verified inside a WSL session, so this pins the portable off-WSL contract every caller relies on instead — an unconditional `wslpath.to_win(p)` never raises, it answers `nil`. |
| `system_job_spec.lua` | `lib.nvim.system.job`: `start`'s line-buffering (terminated/unterminated/CRLF lines, stdout+stderr, no-output, optional callbacks). `start_blocking`: full stdout/exit-code captured synchronously on return, no extra `vim.wait` needed, `opts.stdin` reaching the process. `chain`: success path (one result per step), failure-gating (a failing middle step stops the chain), stdin auto-piped from the previous step's stdout, and an explicit `opts.stdin` overriding that default. |
| `system_lines_spec.lua` | `lib.nvim.system.lines`: `collector()` rejoining a line split across chunks, flushing output that never got its newline, stripping a trailing CR (including a CRLF pair torn across two chunks, which defeats normalizing each chunk on its own) while keeping an interior one, and two streams never completing each other's lines. `buffered()`: the `vim.system` handler shape, per-line callbacks, EOF flush, nil for a nil callback. |
| `ui_kit_spec.lua` | `lib.nvim.ui.kit`: theme/surface/note (phase 1) and toast/input/prompt (phase 2) widget primitives. |
| `ui_hl_persist_spec.lua` | `lib.nvim.ui.hl.persist`: re-applying custom highlights on real `ColorScheme`/`OptionSet background` events (not a stubbed autocmd layer, since the module's whole value is that the wiring is correct). |
| `ui_winhighlight_spec.lua` | `lib.nvim.ui.winhighlight`: `parse`/`serialize`/`merge` round-tripping against a real window and a real `winhighlight` option value, `merge`'s deterministic (sorted, not `pairs()`-order) serialization of a given mapping set, and `update`/`remove` skipping the option write (and the redraw it forces) when nothing actually changed. |
| `copy_to_clipboard_spec.lua` | `lib.nvim.cross.copy_to_clipboard`: the regression where `pcall(vim.fn.setreg, "+", text)` alone reported success even with no clipboard provider present — `setreg` doesn't raise in that case, it just silently does nothing. |
| `ui_list_spec.lua` | `lib.nvim.ui.list`: quickfix/location-list sink — items + title in one call, stack semantics, the open/focus policy, and the empty-list case. |
| `statusline_spec.lua` | `lib.nvim.ui.statusline`: mode resolution against `laststatus`, both drawing strategies, `%` escaping, alignment, detach/restore. |
| `lua_helpers_spec.lua` | `lib.lua.*`: `uuid`, `numeral` (roman/alpha), `diff` (lines/myers), `error` (structured errors, `safe_call`), `yaml`, `time` (presets/format), `strings` (utf8/encoding/distance/format/location/case/wrap + aggregator wiring), `tables.deep_merge`, `dump`, `class` (`new`/`init`/`extend`/override/fallthrough/`include` mixin composition), `context_manager.with` (release-on-success, multi-return + embedded-nil forwarding, release-on-body-error with the error surfacing, acquire-failure short-circuiting release/body entirely), `strings.width` (column-vs-byte counting on a real CJK fixture, combining/zero-width/emoji codepoints, column-dependent tab expansion + `tabstop`, `truncate` on character boundaries incl. a straddling wide char and an over-wide ellipsis, column-aware padding, and the deliberate non-shadowing of `core`'s byte-based `pad_*` on the aggregator). |
| `memo_spec.lua`   | `lib.lua.memo`: `fn`/`memo.memoize` backed by `lib.lua.memo.lru` — cache hits/misses, key derivation, eviction. |
| `nvim_helpers_spec.lua` | New `lib.nvim.*` adapters: module loading, aggregator wiring (`cross`/`window`), `core.first_available`, `normalize` validators, `cross.fs.expand_path`/`mutate`, `buf_win_tab.get_option`/`word_under_cursor`, and `fs` round-trips (`read`, `json`, `collect_recursive`, `scan_roots`). Also `nvim.json.decode`'s error *shape*: the max-depth refusal states its actionable reason and carries no `file:line:` source prefix, since consumers forward `err` verbatim into user-facing notifications. |
| `window_spec.lua` | `lib.nvim.window`: `tag.lua` (set/get/find a tagged window) and `open_scratch_split.lua`. |
| `selection_spec.lua` | `lib.nvim.selection`: line-wise, same-line-character-wise, and multi-line-character-wise Visual selection capture/reselect, driven through real `:normal! v` mode entry (a Visual selection only exists while Vim is actually in Visual mode). |
| `context_spec.lua` | `lib.nvim.buffer.context` / `lib.nvim.window.context`: changedtick/same-event caching, hit/miss stats, `is_normal`/`has_filetype`/`is_processable`, lazy `.lines`, invalidation, invalid-handle fallback. |
| `cache_spec.lua`  | `lib.nvim.cache.disk`: save/load/clear/stats, TTL expiry. `lib.nvim.cache.memory`: namespaces, TTL eviction, changedtick-bound entries, invalidate/clear, and the opt-in/idempotent/toggleable `setup_auto_invalidation`/`disable_auto_invalidation`. |
| `frecency_spec.lua` | `lib.nvim.frecency`: one handle per namespace (two handles on one file overwriting each other on flush is the kind of bug that surfaces months later), scoring, and persistence. |
| `cwd_spec.lua`    | `lib.nvim.fs.chdir` (scopes, normalization, rejection), `lib.nvim.fs.dir_guard` (revert/bypass/update/release, `on_violation`), and `find_root`'s `skip_dirs` / `max_depth` bounds on both the plain and chain-caching paths. |
| `project_store_spec.lua` | `lib.nvim.store.project`: per-project persisted key/value storage. |
| `harvest_spec.lua` | `lib.nvim.harvest.{scope,render,sink,emit}`: collecting and rendering buffer/window "harvest" data through its scope/sink pipeline. |
| `mutate_spec.lua` | `lib.nvim.cross.fs.mutate`: retry-on-transient-error semantics (escalating backoff, `on_retry` hooks) against an injectable `op`, since a real Windows sharing violation (EPERM/EACCES/EBUSY) can't be reliably provoked from inside the test process itself. |
| `lock_spec.lua`   | `lib.nvim.cross.fs.lock`: platform-gated (`supported()` tracks whether the host is Windows) lock/unlock semantics, verified without a real cross-process sharing violation for the same reason `mutate_spec.lua` avoids one. |
| `neotree_watch_spec.lua` | `lib.nvim.neotree.watch`: installs a minimal `neo-tree.sources.filesystem.lib.fs_watch` fake under `package.loaded` (Neo-tree itself is not a test dependency) to control `watch_folder`/`stop_watching` without a real filesystem watcher. |
| `watch_spec.lua`  | `lib.nvim.fs.watch`: a real `fs_event` on a real temp directory — `on_change` fires after a real file write, rapid writes coalesce into fewer callbacks than writes (debounce), `stop()` is idempotent and prevents further callbacks. |
| `composer_spec.lua` | `lib.nvim.bindings.usercmd.composer`: route-tree building, dispatch, `<Tab>` completion, docs generation, and the `check`/`checkhealth`/`notify_check_all` pre-flight helpers, over deliberately minimal command-callback fixtures. |
| `telemetry_wrap_spec.lua` | `lib.strategies.telemetry_wrap`: the one piece of telemetry testing that stays here after `runtime-analysis.telemetry` (moved from this repo, see `docs/modules.md`) took the rest — `setup()` enumerating `require("lib")`'s metatable-hidden aggregate (`control.keys()`) and installing real dispatchers over real lib.nvim functions, a real call counted through them, idempotent re-`setup()` (same instance, not a second live one), and `teardown()` restoring the aggregate byte-for-byte. Requires runtime-analysis.nvim on the runtime path (see `run.lua`'s `add_runtime_analysis()`). |
| `git_spec.lua`    | `lib.nvim.git.info(dir)`: a real repo's branch/version/commit, a non-repo directory's all-nil fields, and a nonexistent directory not raising. `lib.nvim.git.refs(dir, opts)`: this checkout's own refs — a bare local branch, remote branches keeping their prefix, deduplication, `limit`, the group toggles narrowing the list, and a non-repo/nonexistent directory yielding an empty list rather than nil. The module's other ten cwd-reading functions predate this spec and are not covered here. |
| `curl_spec.lua`   | `lib.nvim.net.curl`: all four fetch tiers and `download` against a tiny real `vim.uv` TCP server (no external test dependency); the credential-header redaction contract (`bearer_token`/`auth`/`Authorization`/`Proxy-Authorization`/`Cookie`/`PRIVATE-TOKEN`/`secret_headers` reach the wire but not argv, `config_quote`'s newline escaping, `secret_headers` vs. `opts.headers` dedup); `is_secret_header`'s exact list, including that `x-api-key` is deliberately not on it. **BUG regression (unfixed):** `opts.query` is pinned as still landing in the URL's own argv element today, unprotected the way headers are — see the curl README's "Not covered either" section. |
| `deps_spec.lua`   | `lib.nvim.deps.spec`: `parse_markdown`/`parse_json` (well-formed, missing `bin`/`why`/`pkg`, `required` default, malformed input), `load`'s extension dispatch, `find`/`plugins` over both resolution sources — a fixture on `rtp`, and a fixture reachable *only* through a stubbed `lazy.core.config` (the not-yet-loaded case `runtimepath` cannot see). `deps.pm`: command composition against explicit manager definitions, incl. winget's one-command-per-package split. `deps.install`: `plan` classification and all three `run` refusal paths. `deps.view`: `lines` ordering (required-missing → missing → present), the short-`why` nudge, spec-error section. `deps.health`: `report`/`from_tools` smoke-tested not to error. `deps.status`: the cross-plugin merge over two `rtp` fixtures — a tool declared once carried through intact, a tool declared twice appearing once with both declarers named in `see`, `required` taken from the stricter declarer, `pkg` maps unioned with the first declaration winning a contested key, `bin_alternatives` unioned, and `lines` rendering the merge through the unchanged `deps.view`. `deps.require_tool`: `message` wording (the spec's `why` vs. the bare fallback, the install line for an explicit manager, the "no package known for this host" case), `check` against a binary that really is on PATH (answering with the name it was found under, not `true` — including a tool present only under a `bin_alternatives` spelling, which is why it is a name), a missing one, `silent`, the throttle window swallowing the second report and `throttle_ms = 0` disabling it, `reset` per plugin and wholesale, a plugin shipping no spec at all still answering, and `lines` reaching the same wording by binary name without reporting. |
| `contextmenu_spec.lua` | `lib.nvim.contextmenu`: entry/group/submenu pure data builders, renderer selection (including the fallback for an unknown renderer name), and `bind_buffer`'s wiring. |
| `is_subpath_spec.lua` | `lib.nvim.fs.is_subpath`: whole-segment prefix semantics (both separators, equality included), and the opt-in `opts` canonicalization that makes a comparison survive two differently-spelled paths to the same directory — a Windows drive-letter case mismatch, an 8.3 short name, and (Windows-only) a real substitute drive. |
| `stdpath_config_root_spec.lua` | `lib.nvim.fs.stdpath_config_root`: a directory inside/outside `stdpath("config")`, a name-prefix sibling that is not inside it, the empty-config guard, and the symlinked-dotfiles case — `~/.config/nvim` pointing into a repo, where the raw and canonical spellings differ and only the canonical one is a prefix of the buffer's own path. Also that the resolved spelling is cached per `stdpath("config")` value rather than for the session, so a stub is seen. The symlinked cases need a real directory symlink and skip **loudly** where one cannot be created — raising instead of skipping under `CI` anywhere but Windows. The gate is on the symlink, not the platform: all three CI runners can create one, so all three run these cases; Windows is only the platform where the privilege can be absent. |
| `normkey_spec.lua` | `lib.nvim.fs.normkey`: two spellings of the same path produce the same key, including a path that does not exist *yet* (where `uv.fs_realpath` fails and the old fallback returned the input verbatim — on Windows an unresolved `$TEMP` 8.3 short form keyed differently before `mkdir` than after it), drive-letter uppercasing, and a UNC prefix surviving the duplicate-slash collapse intact. |
| `globbable_spec.lua` | `lib.nvim.fs.globbable`: hands back a spelling of a directory that `vim.fn.glob`/`globpath` read as a path rather than as a pattern — the tilde case (Windows' 8.3 `~1` form is read as a nonexistent user's home directory by `glob`), verified by globbing a real fixture tree through both the raw and resolved spellings and comparing counts. |
| `vregex_spec.lua` | `lib.nvim.vregex`: Vim-regex construction/matching helpers. |
| `range_spec.lua`  | `lib.lua.range`: numeric/position range construction and containment checks. |
| `apply_edits_spec.lua` | `lib.nvim.buffer.apply_edits`: applying a set of (possibly overlapping-in-line, byte-offset-bearing) text edits to a real buffer in the correct order so earlier edits don't invalidate later ones' offsets. |
| `checkpoint_spec.lua` | `lib.nvim.checkpoint`: buffer/window state snapshot and restore. |
| `config_repo_file_spec.lua` | `lib.nvim.config.repo_file`: reading a repo-local config file against real temp files on disk — one fixture per failure mode, plus the allowlist split itself. |
| `relpath_spec.lua` | `lib.nvim.fs.relpath`: no prior coverage existed. Direct/nested child, identical-path `.`, common-ancestor climbing, trailing-separator handling, and (Windows) mixed backslash/forward-slash input. **BUG regression (fixed):** a Windows drive letter differing only in case (`c:` vs. `C:`) used to be read as "no shared root at all" and fell back to returning the absolute path unchanged instead of computing a real relative one — root cause was `root_of()`'s case-sensitive `~=` comparison; fixed by folding just the drive-letter prefix through the same `lib.nvim.cross.fs.separators.drive_upper` helper `normkey` already uses for this. |
| `config_spec.lua` | `lib.config`: an unknown option key is reported before the merge (with a "did you mean" hint) and never stored, a known key alone reports nothing, and an unknown strategy name still falls back to `metatable`. |
| `run.lua`         | Runner: loads every spec, reports results, sets the exit code.  |

## Adding a spec

Create `<name>_spec.lua` returning `function(H) … end` (use `H.eq` / `H.ok` /
`H.tmpfile` / `H.read_lines`) and add its filename to the `specs` list in
`run.lua`.
