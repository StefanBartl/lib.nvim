---@meta
---@module 'lib.lua.class.@types'

---@class Lib.Class
---@field new fun(name: string): table Define a new root class.
---@field include fun(target: table, mixin: table) Copy every function field from `mixin` onto `target`, skipping fields `target` already defines.

return {}
