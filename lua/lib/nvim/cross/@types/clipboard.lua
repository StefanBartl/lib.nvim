---@meta
---@module 'lib.nvim.cross.@types.clipboard'

--- CDX: unreferenced — `cross` exposes no `clipboard` sub-table; the module is
--- `cross.copy_to_clipboard` (a bare function), reachable flat as `lib.copy_to_clipboard`.
---@class Lib.Cross.Clipboard
---@field copy_to_clipboard fun(text: string): boolean

return {}
